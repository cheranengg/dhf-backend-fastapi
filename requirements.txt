# Web API
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic<2

# Core ML stack (model loading/generation)
transformers==4.55.0
peft==0.17.0
# torch/torchvision/torchaudio are installed in Docker via the CUDA index

# Optional local similarity (both are used by ha_infer + optional)
sentence-transformers==5.1.0
faiss-cpu==1.11.0.post1

# Utilities
requests==2.32.4
